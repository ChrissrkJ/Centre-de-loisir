{% extends "::base.html.twig" %}

{% block body %}
<div class="row">
  <div class="col-md-6">
    <h4>Liste des professeurs : </h4>

    <table class="table table-striped table-dark">
      <thead>
        <th>Nom</th>
        <th>Prénom</th>
        <th>Action</th>
      </thead>
      <tbody>
        {% for t in teachers %}
        <tr>
          <td>{{ t.lastname }}</td>
          <td>{{ t.firstname }}</td>
          <td>
            <a href="#" class="btn btn-danger btn-xs">Supprimer</a>
            <a href="#" class="btn btn-warning btn-xs">Modifier</a>
          </td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>

  <div class="col-md-6">
    <h4>Ajouter un professeur: </h4>
    <a href="{{path('teacher_add')}}" class="btn btn-primary" >Cliquez ici >>></a>
  </div>
</div>





{% endblock %}
